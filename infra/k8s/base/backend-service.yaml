apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: codexpong
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: codexpong
spec:
  selector:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: codexpong
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
