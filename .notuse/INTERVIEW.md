> 이 표는 "9개 중 최소 5개"를 **구현 증거**로 연결하기 위한 매핑이다.
> C++ 다형성은 **필수 아님**. 구현 증거로 가져갈 경우에만 v0.12에서 `worker/native/`를 추가한다.

| 면접 항목         | 근거 버전(기존/신규)                           | 이 버전에서 ‘증명’되는 기능                                                 | 증명용 산출물 / 코드 포인트(추천)                                                                                                                      |
| ------------- | -------------------------------------- | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| UTF-8         | **v0.10.0(기존)** + v0.14.0(신규)          | v0.10: `TZ=Asia/Seoul` + `utf8mb4` end-to-end 요구                 | (산출물) UTF-8 회귀테스트: 한글+이모지 닉네임이 **DB↔API↔프론트↔WS** 왕복해도 동일. (코드) `backend/.../EncodingRegressionTest.java`, `frontend/.../i18nUtf8.spec.ts` |
| React         | **v0.1.0(기존)** + v0.14.0(신규)           | v0.1: React/TS 라우팅/페이지 구성                                        | (산출물) 리플레이/잡 UI가 React 컴포넌트로 분리 + 상태/렌더 최적화. (코드) `frontend/src/pages/Replays.tsx`, `Jobs.tsx`, `ReplayViewer.tsx`                        |
| Reflow        | v1.0.0(기존 요구: 성능 이슈 해결)  + v0.14.0(신규) | 긴 리스트/캔버스 UI에서 layout thrash 제거, 가상화/페이지네이션                      | (산출물) DevTools Performance 캡처 "Before/After" + 개선 리포트. (코드) `frontend/src/pages/Leaderboard.tsx`(가상화), `Replays.tsx`(가상화)                 |
| 함수형 패러다임      | v0.14.0(신규)                            | 리플레이 타임라인/플레이어 상태를 **순수 reducer + 불변 업데이트**로 처리                  | (코드) `frontend/src/replay/state/replayReducer.ts`(pure), `selectors.ts`, 유닛테스트                                                            |
| Callback hell | v0.12.0~v0.14.0(신규)                    | "export 생성 → 진행률 수신 → 완료 다운로드"를 **Promise/async** 기반으로 단순화       | (코드) `frontend/src/ws/jobClient.ts`(promise wrapper), `useJobProgress.ts`(hook), 공통 에러 처리                                                 |
| async I/O     | **v0.1.0(기존)** + v0.12.0(신규)           | v0.1: WebSocket 기본 wiring  / v0.12: 큐/워커로 I/O 대기 작업을 요청-응답에서 분리  | (산출물) "요청 즉시 jobId 반환" + "백그라운드 완료" 데모. (코드) `backend/.../JobController.java`에서 즉시 반환, 큐 publish                                          |
| IPC           | v0.12.0(신규)                            | 백엔드 ↔ 워커를 **프로세스 분리 + 메시지 스키마**로 통신                              | (산출물) IPC 스키마 문서 + 재시도/중복처리 규칙. (코드) `backend/.../queue/RedisStreamPublisher.java`, `worker/src/queue_consumer.cpp`                       |
| C++ 다형성       | v0.12.0(신규)                            | 워커 내부를 `IJob`(virtual) + `ExportMp4Job/ThumbnailJob`로 확장 가능하게 구성 | (코드) `worker/src/IJob.hpp`(virtual dtor 포함), `ExportMp4Job.cpp`, `ThumbnailJob.cpp`, `JobRegistry.cpp`                                    |
| IPC           | v0.12.0(신규)                            | 백엔드 ↔ 워커를 **프로세스 분리 + 메시지 스키마**로 통신                              | (산출물) IPC 스키마 문서 + 재시도/중복처리 규칙. (코드) `backend/.../queue/RedisStreamPublisher.java`, `worker/.../RedisStreamsConsumer.(java|kt)`                       |
| C++ 다형성(선택)  | v0.12.0(신규, 선택)                        | (선택) `IJob`(virtual) + 구현체로 job 실행/확장 구조를 증명                   | (코드) `worker/native/IJob.hpp`(virtual dtor 포함), `ExportMp4Job.cpp`, `ThumbnailJob.cpp`, `JobRegistry.cpp`                                    |
| GPU 하드웨어 가속   | v0.13.0(신규)                            | (클라) WebGL 리플레이 렌더러 + (서버) HW 인코딩 옵션 + CPU fallback              | (코드) `frontend/src/replay/renderers/webgl/*` + `canvas2d/*` fallback, `worker`에서 `--hwaccel` 옵션/감지 로직                                     |
